spring:
  application:
    name: springcloud-alibaba-nacos-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        service: ${spring.application.name}

server:
  port: 8021